name: Akuh Final Observation

on:
  schedule:
    - cron: "0 22,3,12 * * *" # 07:00, 12:00, 21:00 JST
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      # ★隠れたv1beta汚染をここで物理的に遮断
      GOOGLE_API_VERSION: v1
      GOOGLE_GENAI_ENDPOINT: https://generativelanguage.googleapis.com
      
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      X_API_KEY: ${{ secrets.X_API_KEY }}
      X_API_SECRET: ${{ secrets.X_API_SECRET }}
      X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
      X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install google-genai tweepy
      - name: Run Akuh
        run: python synergy.py
